# Development/test only. SQL Server Developer edition is not licensed for production use.
# Example password is public and synthetic. Change it before shared DEV use.
# docker-compose.yml deliberately has no password fallback; .env is required.
JORNADA_SQL_SA_PASSWORD=Jornada_Local_2026!ChangeMe
JORNADA_SQL_PORT=14333
JORNADA_SQL_DATABASE=JornadaLocal
