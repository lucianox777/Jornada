name: repair-provider-locks

on:
  push:
    paths:
      - '.github/workflows/_repair-provider-locks.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: Solution
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: true
          fetch-depth: 0
      - uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: '8.0.424'

      - name: Repair transitive provider locks
        run: |
          dotnet restore src/Jornada.Resultado.Api/Jornada.Resultado.Api.csproj --use-lock-file --force-evaluate
          dotnet restore tests/Jornada.Integrador.Tests/Jornada.Integrador.Tests.csproj --use-lock-file --force-evaluate
          dotnet restore Jornada.sln --locked-mode
          mkdir -p .local/lock-repair
          python3 scripts/nuget-lock-gate.py --root . --summary .local/lock-repair/summary.json

      - name: Refresh lock provenance
        shell: bash
        run: |
          python3 - <<'PY'
          import hashlib, json
          from pathlib import Path
          root=Path('.').resolve()
          manifest=root/'config/release/nuget-lock-provenance.json'
          data=json.loads(manifest.read_text(encoding='utf-8'))
          locks=sorted(
              p for p in root.rglob('packages.lock.json')
              if '.local' not in p.parts and 'obj' not in p.parts and 'bin' not in p.parts
          )
          old={row.get('path'):row for row in data.get('locks',[]) if row.get('path')}
          rows=[]
          for path in locks:
              rel=path.relative_to(root).as_posix()
              digest=hashlib.sha256(path.read_bytes()).hexdigest()
              prior=old.get(rel,{})
              rows.append({
                  'path': rel,
                  'origin': 'REGENERATED_OR_VERIFIED_V405_SDK_8_0_424',
                  'sourceSha256': digest,
                  'sha256': digest,
                  'note': prior.get('note') or 'Lock regenerado pelo SDK 8.0.424; promoção exige --force-evaluate sem diff e --locked-mode.',
                  'verificationStatus': 'CI_FORCE_EVALUATE_AND_LOCK_GATE_PASS_V405'
              })
          summary=json.loads((root/'.local/lock-repair/summary.json').read_text(encoding='utf-8'))
          data['locks']=rows
          data['pendingLockCount']=0
          generation=data.setdefault('lockGraphGeneration',{})
          generation['sdk']='8.0.424'
          generation['command']='targeted provider restore --force-evaluate + solution --locked-mode'
          generation['diagnosticRunId']='CI_REPAIRED_TRANSITIVE_PROVIDER_LOCKS'
          generation['lockCount']=len(rows)
          generation['nugetLockGate']='PASS'
          generation['combinedSha256']=summary['combinedSha256']
          manifest.write_text(json.dumps(data,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          PY
          python3 scripts/nuget-lock-provenance-gate.py --root . --summary .local/lock-repair/provenance.json

      - name: Commit repaired locks
        shell: bash
        working-directory: .
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          find Solution -name packages.lock.json -not -path '*/obj/*' -not -path '*/bin/*' -print0 | xargs -0 git add
          git add Solution/config/release/nuget-lock-provenance.json
          if git diff --cached --quiet; then
            echo 'No lock drift remained after targeted restore.'
            exit 1
          fi
          git commit -m 'build: repair transitive PostgreSQL provider locks'
          git push origin HEAD:work/postgresql-adapter
